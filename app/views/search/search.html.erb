<%= params[:query]%>
<br>
<% TWITTER.search(params[:query], result_type: "recent").take(3).collect do |tweet|%>
    <% KAFKA.deliver_message(tweet.full_text, topic: "twitterdata") %>
    <%= tweet.full_text %>
    <br>
    <br>
    <br>
<% end %>