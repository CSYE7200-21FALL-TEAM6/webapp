<head>
</head>

<%= params[:query]%>
<br>
<% TWITTER.search(params[:query], result_type: "recent", tweet_mode: "extended").take(1).collect do |tweet|%>
    <% KAFKA.deliver_message(params[:query] + "%%%%%" + @token + "%%%%%" + tweet.text + "%%%%%" + tweet.favourite_count.to_s + "%%%%%" + tweet.retweet_count.to_s, topic: "twitterdata") %>
    <%= tweet.full_text %>
    <br>
    favorite_count:
    <%= tweet.favourite_count %>
    <br>
    retweet_count:
    <%= tweet.retweet_count %>
    <%= tweet.attrs %>
<% end %>